# D:\model_picture 金属零件缺陷分割训练操作手册

## 项目背景

本项目基于 **STM32MP157DAA1** 工业缺陷检测与分拣系统，目标是检测金属零件（垫圈、冲压件等）表面缺陷，包括划痕、锈蚀、压伤、裂纹、毛刺五类。最终模型将以 INT8 量化形式部署到 STM32MP157（双核 Cortex-A7 @ 800MHz，无 GPU）上运行。

**选型方案**：UNet + MobileNetV2/MobileNetV3（分割方案B）
- 当前已完成：MobileNetV2 主链路（26 MB ONNX）、MobileNetV3 对比链路（14 MB ONNX）
- UNet 的跳跃连接能保留细小缺陷的边界信息
- MobileNetV3-Small backbone 比 V2 更轻，ONNX 体积减小近一半
- **注意**：本项目当前所有 ONNX 均为 UNet 分割模型（输出像素级掩码），MP157 第一阶段主链路还需要单独训练 **MobileNetV3-Small 分类模型**（输出 good/bad）

**本地训练环境**：
- GPU：NVIDIA GeForce RTX 4060 Laptop（8GB 显存）
- CUDA Driver：12.5（nvidia-smi 显示）
- PyTorch：2.6.0+cu124（CUDA Runtime 12.4，兼容 12.5 驱动）
- Python：3.9
- Conda 环境：`D:\model_picture\defect-unet`

---

## 1. 项目目录结构

```
D:\model_picture\
├── defect-unet\              ← Conda 虚拟环境（Python 3.9 + PyTorch）
├── datasets\                 ← 训练数据（转换后）
│   ├── train\               ← 分割数据集（像素级掩码标注）
│   │   ├── images\           ← 训练原图（70%）
│   │   └── masks\            ← 训练掩码 PNG（70%）
│   ├── val\                  ← 验证集
│   │   ├── images\           ← 验证原图（15%）
│   │   └── masks\            ← 验证掩码 PNG（15%）
│   └── test\                 ← 测试集
│       ├── images\           ← 测试原图（15%）
│       └── masks\            ← 测试掩码 PNG（15%）
├── datasets_classify\        ← 【新增】分类数据集（整图 good/bad 标签）
│   ├── train\good\            ← 良品图
│   ├── train\bad\             ← 缺陷图
│   ├── val\good\             ← 验证良品
│   ├── val\bad\              ← 验证缺陷
│   └── class_labels.txt      ← 类别映射文件
├── raw_images\               ← Labelme 原始标注 JSON + 原图
├── converted_images\         ← 转换后的原图（convert_labelme.py 生成）
├── converted_masks\          ← 转换后的掩码（convert_labelme.py 生成）
├── checkpoints\              ← MobileNetV2 UNet 分割模型
│   ├── best_model.pth        ← 最佳模型（~77 MB）
│   └── defect_unet.onnx      ← 导出 ONNX（~25 MB）
├── checkpoints_mobilenetv3\  ← MobileNetV3 UNet 分割对比实验
│   ├── best_model.pth        ← 最佳模型（~42 MB）
│   └── defect_unet_mobilenetv3.onnx  ← 导出 ONNX（~14 MB）
├── logs\                     ← TensorBoard 日志
├── dataset.py                ← 分割数据集加载代码
├── train.py                  ← 分割训练脚本
├── export_onnx.py            ← ONNX 导出脚本
├── convert_labelme.py        ← Labelme 标注转换脚本
├── capture.py                ← UVC 摄像头截图脚本
├── train_classify.py         ← 【新增】分类模型训练脚本
├── export_classify_onnx.py   ← 【新增】分类模型 ONNX 导出
└── 操作手册.md               ← 本文档
```

---

## 2. 每次开机如何运行

### 核心要点

本项目的conda环境在 `D:\model_picture\defect-unet`，所有Python脚本都用完整路径运行：

```
D:\model_picture\defect-unet\python.exe  <脚本名>
```

不需要先激活conda环境，直接用完整路径即可。以下所有命令都基于这种方式。

### 方法一：CMD 命令行

```bash
cd /d D:\model_picture
D:\model_picture\defect-unet\python.exe <脚本名>
```

如果CMD中conda可用，也可以先激活再运行：
```bash
E:\Miniconda\condabin\conda_hook.bat
conda activate D:\model_picture\defect-unet
cd /d D:\model_picture
python <脚本名>
```

### 方法二：VSCode 终端（推荐）

1. 打开 VSCode
2. 文件 → 打开文件夹 → 选择 `D:\model_picture`
3. 按 `Ctrl + \`` 打开终端
4. 直接运行（无需激活conda）：
   ```powershell
   D:\model_picture\defect-unet\python.exe <脚本名>
   ```

**注意**：VSCode默认用PowerShell终端，`conda_hook.bat` 在PowerShell中不生效。
直接用 `D:\model_picture\defect-unet\python.exe` 是最简单可靠的方式。

### 常用命令速查

```powershell
# === 环境检查 ===
D:\model_picture\defect-unet\python.exe -c "import torch; print(torch.__version__, torch.cuda.is_available())"  # 检查PyTorch+CUDA
D:\model_picture\defect-unet\python.exe -c "import segmentation_models_pytorch as smp; print(smp.__version__)"  # 检查SMP版本

# === 数据采集 ===
D:\model_picture\defect-unet\python.exe capture.py --save_dir ./raw_images/good --camera_id 0  # 拍良品图
D:\model_picture\defect-unet\python.exe capture.py --save_dir ./raw_images/scratch --camera_id 0  # 拍划痕缺陷图

# === 分割数据标注（Labelme 像素级掩码） ===
D:\model_picture\defect-unet\python.exe -m labelme ./raw_images --labels scratch,rust,dent,crack,burr  # 启动标注
D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/good ./converted_images ./converted_masks  # 转换良品标注
D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/scratch ./converted_images ./converted_masks  # 转换划痕标注
D:\model_picture\defect-unet\python.exe convert_labelme.py split ./converted_images ./converted_masks ./datasets  # 划分 train/val/test

# === 分割模型训练（UNet + MobileNetV2/V3） ===
D:\model_picture\defect-unet\python.exe train.py  # 默认参数训练
D:\model_picture\defect-unet\python.exe train.py --data_dir ./datasets --epochs 100 --batch_size 8  # 标准训练
D:\model_picture\defect-unet\python.exe train.py --data_dir ./datasets --epochs 100 --batch_size 8 --encoder tu-mobilenetv3_small_100.lamb_in1k --checkpoint_dir ./checkpoints_mobilenetv3  # V3对比实验
D:\model_picture\defect-unet\python.exe train.py --resume auto  # 断点续训

# === 分类模型训练（MobileNetV3-Small，第一阶段优先） ===
D:\model_picture\defect-unet\python.exe train_classify.py --data_dir ./datasets_classify --epochs 100 --batch_size 16  # 标准训练（需先准备 good/bad 文件夹）
D:\model_picture\defect-unet\python.exe train_classify.py --resume auto  # 分类模型续训

# === 分割模型导出 ONNX ===
D:\model_picture\defect-unet\python.exe export_onnx.py --checkpoint ./checkpoints/best_model.pth --output ./checkpoints/defect_unet.onnx --num_classes 6 --encoder mobilenet_v2  # V2主链路
D:\model_picture\defect-unet\python.exe export_onnx.py --checkpoint ./checkpoints_mobilenetv3/best_model.pth --output ./checkpoints_mobilenetv3/defect_unet_mobilenetv3.onnx --num_classes 6 --encoder tu-mobilenetv3_small_100.lamb_in1k  # V3对比链路

# === 分类模型 ONNX 导出 ===
D:\model_picture\defect-unet\python.exe export_classify_onnx.py --checkpoint ./checkpoints_classify/best_model.pth --output ./checkpoints_classify/defect_classifier.onnx  # 分类模型导出

# === 分割模型 INT8 量化（静态量化，推荐生产部署） ===
D:\model_picture\defect-unet\python.exe quantize_segment_int8.py --onnx_input ./checkpoints/defect_unet.onnx --onnx_output ./checkpoints/defect_unet_int8.onnx --calib_dir ./datasets/val/images --num_calib 100  # MobileNetV2 UNet 静态量化
D:\model_picture\defect-unet\python.exe quantize_segment_int8.py --onnx_input ./checkpoints_mobilenetv3/defect_unet_mobilenetv3.onnx --onnx_output ./checkpoints_mobilenetv3/defect_unet_mobilenetv3_int8.onnx --calib_dir ./datasets/val/images --num_calib 100  # MobileNetV3 UNet 静态量化

# === 分割模型 INT8 量化（动态量化，快速测试） ===
D:\model_picture\defect-unet\python.exe quantize_segment_dynamic.py --onnx_input ./checkpoints/defect_unet.onnx --onnx_output ./checkpoints/defect_unet_dynamic_int8.onnx  # 无需校准数据

# === 分类模型 INT8 量化 ===
D:\model_picture\defect-unet\python.exe quantize_classify_int8.py --onnx_input ./checkpoints_classify/defect_classifier.onnx --onnx_output ./checkpoints_classify/defect_classifier_int8.onnx --calib_dir ./datasets_classify/val --num_calib 100  # 分类静态量化
D:\model_picture\defect-unet\python.exe quantize_classify_dynamic.py --onnx_input ./checkpoints_classify/defect_classifier.onnx --onnx_output ./checkpoints_classify/defect_classifier_dynamic_int8.onnx  # 分类动态量化

# === 分类模型本地推理测试 ===
D:\model_picture\defect-unet\python.exe infer_classify.py --mode image --input ./test.jpg --model ./checkpoints_classify/defect_classifier.onnx  # 单张图片推理
D:\model_picture\defect-unet\python.exe infer_classify.py --mode camera --camera_id 0 --model ./checkpoints_classify/defect_classifier.onnx  # 摄像头实时推理
D:\model_picture\defect-unet\python.exe infer_classify.py --mode batch --input ./test_images/ --model ./checkpoints_classify/defect_classifier.onnx  # 批量推理

# === 监控与调试 ===
D:\model_picture\defect-unet\python.exe -m tensorboard.main --logdir D:\model_picture\logs  # 分割训练监控
D:\model_picture\defect-unet\python.exe -m tensorboard.main --logdir D:\model_picture\logs_classify  # 分类训练监控
```

---

## 3. 训练数据规范

### 3.1 类别定义

| 像素值 | 类别 | 英文标签 | 说明 |
|---|---|---|---|
| 0 | 背景 | background | 正常区域、无缺陷 |
| 1 | 划痕 | scratch | 表面线性划伤 |
| 2 | 锈蚀 | rust | 表面氧化锈斑 |
| 3 | 压伤 | dent | 凹陷、磕碰痕迹 |
| 4 | 裂纹 | crack | 细小裂纹 |
| 5 | 毛刺 | burr | 边缘冲压毛刺 |

### 3.2 数据拍摄要求

| 要求 | 说明 |
|---|---|
| 用实际工位的 UVC 摄像头拍 | 不要用手机，镜头畸变和色彩不同 |
| 用实际工位的光源 | 补光角度、色温必须一致 |
| 分辨率 640x480 | 你的UVC摄像头默认分辨率 |
| 零件居中、固定焦距 | 手动对焦后锁定 |
| 每张图只有一个零件 | 避免多零件干扰 |
| 不要后期调色 | 模型要学原始特征 |

### 3.3 数量要求

| 类别 | 最少 | 推荐 | 理想 |
|---|---|---|---|
| 良品 | 500张 | 1000张 | 2000+张 |
| 每类缺陷 | 150张 | 300-500张 | 500+张 |

### 3.4 原始数据存放

拍摄后的原始图片和 Labelme 标注 JSON 按缺陷类型分目录存放在 `raw_images` 下：

```
D:\model_picture\raw_images\
├── good\
│   ├── 0000.jpg          ← 良品原图
│   ├── 0000.json         ← 良品Labelme标注（空标注）
│   └── ...
├── scratch\
│   ├── 0000.jpg          ← 划痕原图
│   ├── 0000.json         ← 划痕Labelme标注
│   └── ...
├── rust\
│   └── ...
├── dent\
│   └── ...
├── crack\
│   └── ...
└── burr\
    └── ...
```

**命名规则**：
- capture.py 自动用4位数字编号：`0000.jpg`、`0001.jpg`、...
- 原图和对应 JSON 同名不同后缀
- 图片格式：`.jpg`
- 掩码格式：`.png`（转换后自动生成）

---

## 4. 数据标注流程（Labelme）

### 4.1 启动 Labelme

```powershell
D:\model_picture\defect-unet\python.exe -m labelme ./raw_images --labels scratch,rust,dent,crack,burr
```

### 4.2 标注操作

1. 在 Labelme 左侧选择一张图片
2. 右侧点击 "Create Polygons"（创建多边形）
3. 沿缺陷边缘点击画多边形，双击结束
4. 在弹出对话框中选择标签名（如 scratch）
5. 有多个缺陷就重复步骤 2-4
6. 良品图片直接保存（空标注 = 全背景）
7. File → Save 保存 JSON

### 4.3 标注质量要求

| 要求 | 说明 |
|---|---|
| 边界贴合缺陷 | 不要画太大也不要画太小 |
| 同一缺陷一个标注 | 不要把一个缺陷拆成多个多边形 |
| 良品也要保存 JSON | 空标注 = 全背景，防止误检 |
| 标签名必须严格匹配 | 只能用 scratch/rust/dent/crack/burr |

---

## 5. 数据转换与划分

### 5.1 转换 Labelme JSON 为掩码 PNG

```powershell
cd D:\model_picture

# 按缺陷类型子目录分别转换
D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/good ./converted_images ./converted_masks
D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/scratch ./converted_images ./converted_masks
D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/rust ./converted_images ./converted_masks
D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/dent ./converted_images ./converted_masks
D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/crack ./converted_images ./converted_masks
D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/burr ./converted_images ./converted_masks
```

执行后会生成：
- `converted_images/` —— 原图副本
- `converted_masks/` —— 掩码 PNG（像素值 = 类别 ID）

### 5.2 划分训练/验证/测试集

```powershell
D:\model_picture\defect-unet\python.exe convert_labelme.py split ./converted_images ./converted_masks ./datasets
```

划分比例：**70% 训练 / 15% 验证 / 15% 测试**

执行后 `datasets/` 下自动生成 train/val/test 子目录，各含 images 和 masks。

---

## 6. 训练操作

### 6.1 在 VSCode 中开始训练

1. 打开 VSCode
2. 文件 → 打开文件夹 → 选择 `D:\model_picture`
3. 按 `Ctrl + \`` 打开终端
4. 直接运行：
   ```powershell
   D:\model_picture\defect-unet\python.exe train.py
   ```

### 6.2 标准训练命令

```powershell
D:\model_picture\defect-unet\python.exe train.py --data_dir ./datasets --epochs 100 --batch_size 8
```

### 6.3 完整参数说明

| 参数 | 默认值 | 说明 |
|---|---|---|
| `--data_dir` | `./datasets` | 数据集根目录 |
| `--num_classes` | 6 | 类别数（背景+5种缺陷） |
| `--batch_size` | 8 | 批大小，显存不够改 4 或 2 |
| `--epochs` | 100 | 训练轮数，先跑 100 轮看效果 |
| `--lr` | 1e-3 | 学习率 |
| `--encoder` | mobilenet_v2 | backbone 名称（V3对比用 `tu-mobilenetv3_small_100.lamb_in1k`） |
| `--checkpoint_dir` | `./checkpoints` | 模型保存目录 |
| `--log_dir` | `./logs` | TensorBoard 日志目录 |
| `--num_workers` | 4 | 数据加载线程数 |
| `--resume` | `""` | 断点续训路径；填 `auto` 则自动加载 last_checkpoint.pth |
| `--patience` | 12 | Early Stopping 耐心轮数，0 表示关闭 |
| `--seed` | 42 | 随机种子，提高可复现性 |
| `--class_weights` | auto | 类别权重策略：none 不加权，auto 按训练 mask 自动估计 |

### 6.4 显存不够时调整

| 现象 | 解决 |
|---|---|
| CUDA Out of Memory | `--batch_size 4` |
| 4 也不够 | `--batch_size 2` |
| 还是不够 | 在 dataset.py 中把 Resize 改为 160x160 |

你的 RTX 4060 8GB 显存，batch_size=8 应该没问题。

### 6.5 用 TensorBoard 监控训练

另开一个终端：
```powershell
D:\model_picture\defect-unet\python.exe -m tensorboard.main --logdir D:\model_picture\logs
```

浏览器打开 **http://localhost:6006** 查看：
- loss 曲线（训练/验证）
- mIoU 曲线
- 每个类别的 IoU 曲线

---

## 7. 训练效果评估

### 7.1 核心指标：IoU（交并比）

IoU = 预测正确的缺陷像素 / (预测缺陷像素 + 真实缺陷像素)
mIoU = 所有类别 IoU 的平均值

**关键：不要只看 mIoU，背景 IoU 会拉高整体值，重点看缺陷类别的 IoU。**

### 7.2 效果等级

| 指标 | 可用 | 较好 | 很好 |
|---|---|---|---|
| mIoU | > 0.45 | > 0.60 | > 0.70 |
| 划痕 IoU | > 0.40 | > 0.55 | > 0.65 |
| 锈蚀 IoU | > 0.50 | > 0.65 | > 0.75 |
| 裂纹 IoU | > 0.30 | > 0.45 | > 0.60 |
| 压伤 IoU | > 0.35 | > 0.50 | > 0.65 |
| 毛刺 IoU | > 0.40 | > 0.55 | > 0.70 |

### 7.3 训练输出示例

```
===== Epoch 10/100 =====
训练 Loss: 0.4521 | mIoU: 0.5123
验证 Loss: 0.3892 | mIoU: 0.5634
学习率: 0.000950
  background: train_iou=0.9200 val_iou=0.9000
  scratch:    train_iou=0.4500 val_iou=0.4000
  rust:       train_iou=0.5500 val_iou=0.5000
  dent:       train_iou=0.4000 val_iou=0.3500
  crack:      train_iou=0.3000 val_iou=0.2500
  burr:       train_iou=0.4500 val_iou=0.4000
★ 保存最佳模型 mIoU=0.5634
```

---

## 8. 效果不好时的调整方法

### 8.1 常见问题速查

| 问题 | 原因 | 解决方法 |
|---|---|---|
| 损失不下降 | 学习率太大或太小 | `--lr 5e-4` 或 `--lr 2e-3` |
| 损失剧烈震荡 | 学习率太大 | `--lr 5e-4 --batch_size 4` |
| 训练好但验证差 | 过拟合 | 增加数据量、增加数据增强 |
| 某类缺陷 IoU=0 | 该类样本太少 | 专门补充该类样本 200+张 |
| 掩码全黑/全 0 | 掩码格式错误 | 用图像查看器检查掩码 PNG 像素值 |
| 掩码边界粗糙 | 分割头不够精细 | 换 encoder_depth=5（改 train.py） |
| 整体 IoU 偏低 | 数据量不够 | 每类至少 300 张 |

### 8.2 分步调优策略

```
训练结果不理想？
  │
  ├── 1. 先看数据
  │     ├── 某类样本 < 150张 → 补充该类样本
  │     ├── 良品样本 < 500张 → 补充良品样本
  │     └── 掩码标注不准 → 重新标注边界
  │
  ├── 2. 再调参数
  │     ├── lr 从 1e-3 → 5e-4
  │     ├── epochs 100 → 150（多训几轮）
  │     └── batch_size 8 → 4（小 batch 有时更好）
  │
  ├── 3. 改损失函数
  │     ├── 缺陷面积小 → 增大 Dice 权重（改 train.py 中 0.5 → 1.0）
  │     └── 类别极度不平衡 → 加 Focal Loss
  │
  ├── 4. 换模型
  │     ├── mobilenet_v3_small → mobilenet_v3_large（更强但更大）
  │     └── smp.Unet → smp.UnetPlusPlus（更精细的解码器）
  │
  └── 5. 仍不达标
        └── 考虑方案A（MobileNetV3+LR-ASPP）
            或增加注意力机制
```

### 8.3 从检查点恢复训练

训练中断后可从保存的检查点恢复：

```powershell
# 自动加载 last_checkpoint.pth 续训
D:\model_picture\defect-unet\python.exe train.py --resume auto

# 或指定具体检查点
D:\model_picture\defect-unet\python.exe train.py --resume ./checkpoints/checkpoint_epoch20.pth
```

> 注意：续训时 checkpoint 中的 `num_classes` 和 `encoder` 必须与当前命令行参数一致，否则报错。

---

## 9. 模型导出

训练完成后，最佳模型在 `./checkpoints/best_model.pth`。

### 9.1 导出分割模型 ONNX

**MobileNetV2 主链路（默认）**：
```powershell
D:\model_picture\defect-unet\python.exe export_onnx.py --checkpoint ./checkpoints/best_model.pth --output ./checkpoints/defect_unet.onnx --num_classes 6 --encoder mobilenet_v2
```

**MobileNetV3 对比链路**：
```powershell
D:\model_picture\defect-unet\python.exe export_onnx.py --checkpoint ./checkpoints_mobilenetv3/best_model.pth --output ./checkpoints_mobilenetv3/defect_unet_mobilenetv3.onnx --num_classes 6 --encoder tu-mobilenetv3_small_100.lamb_in1k
```

**实际 ONNX 大小对比**：
| 骨干 | ONNX 大小 | 说明 |
|------|----------|------|
| MobileNetV2 | **~25 MB** | 当前主链路 |
| MobileNetV3-Small | **~14 MB** | 更轻量，推荐用于 MP157 |

### 9.2 导出分类模型 ONNX（新增）

分类模型由 `train_classify.py` 训练，导出命令：
```powershell
D:\model_picture\defect-unet\python.exe export_classify_onnx.py --checkpoint ./checkpoints_classify/best_model.pth --output ./checkpoints_classify/defect_classifier.onnx
```

**分类模型预估大小**：
| 模型 | FP32 ONNX | INT8 量化后 |
|------|----------|------------|
| MobileNetV3-Small 分类 | **~5 MB** | **~1~2 MB** |

### 9.3 后续部署

ONNX 模型导出后，需要在 Linux 环境下做 **INT8 量化**（如 ONNXRuntime quantization 或 NCNN），然后部署到 STM32MP157。

**部署策略**：
- **第一阶段主链路**：分类模型 INT8（~1.5 MB）做 good/bad 判定
- **第二阶段可视化**：分割模型 INT8（~3~5 MB）输出缺陷掩码图
- **不要直接用 FP32 上板**：分割 FP32（14 MB）推理太慢（>200ms）

---

## 10. 完整操作流程总结

### 10.1 分割模型训练流程（现有）

```
1. 打开终端，进入项目目录
   cd D:\model_picture
        │
2. 采集数据（用实际工位摄像头+光源拍摄）
   D:\model_picture\defect-unet\python.exe capture.py --save_dir ./raw_images/good --camera_id 0
   → 存到 raw_images/各子目录
        │
3. 标注数据（Labelme 标像素级掩码）
   D:\model_picture\defect-unet\python.exe -m labelme ./raw_images --labels scratch,rust,dent,crack,burr
   → 在 raw_images/ 下生成同名 JSON
        │
4. 转换标注（按子目录分别转换）
   D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/good ./converted_images ./converted_masks
   ... 其他类别同理
        │
5. 划分数据集
   D:\model_picture\defect-unet\python.exe convert_labelme.py split ./converted_images ./converted_masks ./datasets
        │
6. 开始训练
   D:\model_picture\defect-unet\python.exe train.py --data_dir ./datasets --epochs 100 --batch_size 8
        │
7. 监控训练（另开终端）
   D:\model_picture\defect-unet\python.exe -m tensorboard.main --logdir D:\model_picture\logs
   → 浏览器打开 http://localhost:6006
        │
8. 评估效果
   → 看 mIoU 和各类 IoU
   → 效果不好 → 参见第 8 节调优
        │
9. 导出 ONNX
   D:\model_picture\defect-unet\python.exe export_onnx.py --checkpoint ./checkpoints/best_model.pth
        │
10. 部署到 STM32MP157
    → INT8 量化 → 板上推理
```

### 10.2 分类模型训练流程（新增，第一阶段优先）

分类数据标注成本远低于分割（只需标整图 good/bad）：

```
1. 拍照采集
   D:\model_picture\defect-unet\python.exe capture.py --save_dir ./raw_images_classify/good --camera_id 0
   D:\model_picture\defect-unet\python.exe capture.py --save_dir ./raw_images_classify/bad --camera_id 0
   → 每张图只标一个类别标签，不需要画轮廓
        │
2. 整理分类数据集
   按 good/bad 分类存放为：
   datasets_classify/
   ├── train/good/     ← 训练良品图
   ├── train/bad/      ← 训练缺陷图
   ├── val/good/
   └── val/bad/
        │
3. 训练分类模型
   D:\model_picture\defect-unet\python.exe train_classify.py --data_dir ./datasets_classify --epochs 100 --batch_size 16
        │
4. 导出分类 ONNX
   D:\model_picture\defect-unet\python.exe export_classify_onnx.py --checkpoint ./checkpoints_classify/best_model.pth
        │
5. INT8 量化后部署到 MP157
   → 单帧推理 ~20~40ms，做主链路良坏判定
```

### 10.3 MP157 部署模型选择建议

| 阶段 | 模型 | 角色 | 优先级 |
|------|------|------|--------|
| **第一阶段** | MobileNetV3-Small INT8 分类 | **主链路良坏判定** | P0 |
| 第二阶段 | UNet + MobileNetV3 INT8 | 缺陷可视化/掩码输出 | P1 |
| 备选 | LR-ASPP INT8 | 更轻量分割（如果 UNet 太重） | P2 |

---

## 11. VSCode 操作说明

### 11.1 首次配置

1. 打开 VSCode
2. 安装 Python 扩展（Extensions → 搜索 Python → Install）
3. 文件 → 打开文件夹 → `D:\model_picture`
4. Ctrl+Shift+P → 输入 "Python: Select Interpreter"
5. 选择 `D:\model_picture\defect-unet\python.exe`

### 11.2 运行训练

**方法一：终端命令行（推荐）**

1. Ctrl+` 打开终端
2. 输入：
   ```powershell
   D:\model_picture\defect-unet\python.exe train.py
   ```

**方法二：VSCode 运行按钮**

1. 在 VSCode 中打开 `train.py`
2. 右上角会出现 ▶ 运行按钮
3. 点击旁边的 ▼ 下拉箭头 → "Add Configuration"
4. 在弹出的 launch.json 中配置参数：
   ```json
   {
     "name": "Train Defect UNet",
     "type": "debugpy",
     "request": "launch",
     "program": "${workspaceFolder}/train.py",
     "args": ["--data_dir", "./datasets", "--epochs", "100", "--batch_size", "8"],
     "console": "integratedTerminal",
     "python": "D:\\model_picture\\defect-unet\\python.exe"
   }
   ```
5. 配置好后，直接点击 ▶ 运行或 F5 调试

**方法三：Code Runner 扩展**

1. 安装 "Code Runner" 扩展
2. 打开 train.py
3. 右上角点击 ▶ Run Code
   - 注意：需要先确保终端中 conda 环境已激活

---

## 12. 常见问题排查

| 问题 | 原因 | 解决 |
|---|---|---|
| `conda` 命令找不到 | PowerShell不支持conda_hook.bat | 直接用 `D:\model_picture\defect-unet\python.exe` 运行脚本 |
| `torch.cuda.is_available()` 返回 False | CUDA 版本不匹配 | 确认安装了 cu124 版 PyTorch |
| `No module named smp` | 用了系统Python而非conda环境 | 用完整路径 `D:\model_picture\defect-unet\python.exe` |
| `cv2.imshow` 报错 "function not implemented" | 装了opencv-python-headless（无GUI版） | `pip uninstall opencv-python-headless`，重装 `opencv-python` |
| CUDA Out of Memory | batch_size 太大 | 改 `--batch_size 4` |
| Loss=NaN | 学习率太大 | 改 `--lr 1e-4` |
| 图片和掩码数量不一致 | 转换时部分失败 | 检查 raw_images 中的 JSON 是否完整 |
| Labelme 中文乱码 | 编码问题 | 用 `--labels` 参数指定英文标签 |
| BrokenPipeError | Windows多进程问题 | 改 `--num_workers 0` |

---

## 13. 附录：完整命令速查表（带参数详解）

以下所有命令均使用完整路径 `D:\model_picture\defect-unet\python.exe` 运行，无需激活 conda。

### 13.1 环境检查

```powershell
# 检查 PyTorch 版本 + CUDA 是否可用
# 输出示例: 2.6.0+cu124 True → 表示 PyTorch 正常，GPU 可用
D:\model_picture\defect-unet\python.exe -c "import torch; print(torch.__version__, torch.cuda.is_available())"

# 检查 SMP (segmentation-models-pytorch) 版本
# 输出示例: 0.5.0 → 分割模型库正常
D:\model_picture\defect-unet\python.exe -c "import segmentation_models_pytorch as smp; print(smp.__version__)"
```

### 13.2 数据采集

```powershell
# capture.py 参数说明:
#   --save_dir  : 截图保存目录，按类别分子目录（good/scratch/rust...）
#   --camera_id : 摄像头编号，0=第一个摄像头，外接UVC通常是1或2，黑屏就换
#   --quality   : JPEG 保存质量，默认95，越高画质越好文件越大
D:\model_picture\defect-unet\python.exe capture.py --save_dir ./raw_images/good --camera_id 0
D:\model_picture\defect-unet\python.exe capture.py --save_dir ./raw_images/scratch --camera_id 0
```

### 13.3 分割数据标注（Labelme 像素级掩码）

```powershell
# 启动 Labelme 标注工具
# 参数说明:
#   ./raw_images        : Labelme 打开的目录，里面包含 good/scratch/... 子目录
#   --labels           : 缺陷类别标签名，英文逗号分隔，必须和 convert_labelme.py 里的 LABEL_MAP 一致
D:\model_picture\defect-unet\python.exe -m labelme ./raw_images --labels scratch,rust,dent,crack,burr

# 转换 Labelme JSON → PNG 掩码（按缺陷类型子目录分别转换）
# 参数说明:
#   convert            : 子命令，表示执行"转换"操作
#   ./raw_images/good  : 输入目录，包含 .jpg + .json 文件对
#   ./converted_images : 输出原图副本目录（所有类别的图合并到这里）
#   ./converted_masks  : 输出掩码目录（PNG，像素值=类别ID）
D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/good ./converted_images ./converted_masks
D:\model_picture\defect-unet\python.exe convert_labelme.py convert ./raw_images/scratch ./converted_images ./converted_masks

# 划分训练/验证/测试集
# 参数说明:
#   split              : 子命令，表示执行"划分"操作
#   ./converted_images : 合并后的原图目录
#   ./converted_masks  : 合并后的掩码目录
#   ./datasets         : 输出目录，自动生成 train/val/test + images/masks 子目录
#   划分比例          : 默认 70% 训练 / 15% 验证 / 15% 测试
D:\model_picture\defect-unet\python.exe convert_labelme.py split ./converted_images ./converted_masks ./datasets
```

### 13.4 分割模型训练（UNet + MobileNetV2/V3）

```powershell
# 默认参数训练（数据集在当前目录 ./datasets，100轮，batch_size=8）
D:\model_picture\defect-unet\python.exe train.py

# 标准训练命令（显式指定所有关键参数）
# 参数说明:
#   --data_dir        : 数据集根目录，需包含 train/images, train/masks, val/images, val/masks
#   --epochs          : 训练总轮数，先从100轮开始看效果
#   --batch_size      : 每批次样本数，RTX 4060 8GB 可用8，显存不够改4或2
D:\model_picture\defect-unet\python.exe train.py --data_dir ./datasets --epochs 100 --batch_size 8

# MobileNetV3-Small 对比实验（使用 SMP 0.5.0 的 timm 通用入口）
# 参数说明:
#   --encoder         : backbone 名称，默认 mobilenet_v2；V3 用 tu-mobilenetv3_small_100.lamb_in1k
#   --checkpoint_dir  : 模型保存目录，V3 实验单独存放避免覆盖 V2 结果
D:\model_picture\defect-unet\python.exe train.py --data_dir ./datasets --epochs 100 --batch_size 8 --encoder tu-mobilenetv3_small_100.lamb_in1k --checkpoint_dir ./checkpoints_mobilenetv3

# 断点续训（自动加载 checkpoint_dir/last_checkpoint.pth）
# 参数说明:
#   --resume          : 续训路径，"auto"=自动找 last_checkpoint.pth；也可指定具体文件路径
D:\model_picture\defect-unet\python.exe train.py --resume auto
```

### 13.5 分类模型训练（MobileNetV3-Small，第一阶段优先）

```powershell
# 标准训练命令
# 参数说明:
#   --data_dir        : 分类数据集根目录，需包含 train/good, train/bad, val/good, val/bad
#   --epochs          : 训练总轮数
#   --batch_size      : 每批次样本数，分类模型比分割轻，可用16（RTX 4060 够用）
#   注意             : 分类数据不需要 Labelme 标注，只需按 good/bad 文件夹分类存放图片
D:\model_picture\defect-unet\python.exe train_classify.py --data_dir ./datasets_classify --epochs 100 --batch_size 16

# 分类模型断点续训
D:\model_picture\defect-unet\python.exe train_classify.py --resume auto
```

### 13.6 分割模型导出 ONNX

```powershell
# MobileNetV2 主链路导出
# 参数说明:
#   --checkpoint  : 训练好的 .pth 权重文件路径
#   --output      : 导出的 ONNX 文件路径
#   --num_classes : 类别数，含背景（6 = 背景+5缺陷；Severstal 测试集用5 = 背景+4缺陷）
#   --encoder     : 必须和训练时使用的 backbone 完全一致，否则权重加载失败
D:\model_picture\defect-unet\python.exe export_onnx.py --checkpoint ./checkpoints/best_model.pth --output ./checkpoints/defect_unet.onnx --num_classes 6 --encoder mobilenet_v2

# MobileNetV3 对比链路导出（输出到独立目录，避免覆盖）
D:\model_picture\defect-unet\python.exe export_onnx.py --checkpoint ./checkpoints_mobilenetv3/best_model.pth --output ./checkpoints_mobilenetv3/defect_unet_mobilenetv3.onnx --num_classes 6 --encoder tu-mobilenetv3_small_100.lamb_in1k
```

### 13.7 分类模型导出 ONNX

```powershell
# 分类模型 ONNX 导出
# 参数说明:
#   --checkpoint  : 分类模型 .pth 权重路径（由 train_classify.py 训练产出）
#   --output      : 导出的 ONNX 文件路径
#   --num_classes : 默认 2 (good/bad)，和训练时一致
# 预估产出       : FP32 ~5 MB，INT8 量化后 ~1-2 MB，适合 MP157 主链路部署
D:\model_picture\defect-unet\python.exe export_classify_onnx.py --checkpoint ./checkpoints_classify/best_model.pth --output ./checkpoints_classify/defect_classifier.onnx
```

### 13.8 TensorBoard 监控

```powershell
# 监控分割训练（loss/mIoU/各类IoU 曲线）
# 参数说明:
#   --logdir  : TensorBoard 日志目录，和 train.py 的 --log_dir 参数对应
# 打开方式  : 浏览器访问 http://localhost:6006
D:\model_picture\defect-unet\python.exe -m tensorboard.main --logdir D:\model_picture\logs

# 监控分类训练（loss/acc/lr 曲线）
D:\model_picture\defect-unet\python.exe -m tensorboard.main --logdir D:\model_picture\logs_classify
```
